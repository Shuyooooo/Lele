# 🧠 Memory Rewrite Demo 2.0 策划文档

> 版本目标：在不制造“认知扭曲”的前提下，让玩家理解并完成
> **「过去是可修改的数据 -> 现在是被验证的结果」** 的因果机制。

---

## ✦ 一、Demo 2.0 一句话目标

做出一个“修改过去记录 -> 现在必须靠记忆手动输入 -> 结果被解锁”的最小谜题闭环。

---

## ✦ 二、核心母题（沿用）

> **“回忆不是再现，而是被当前状态重构的数据。”**

在 Demo 2.0 中，把“重构”落到可理解的数据层：
- Past 写入/修正一段“记录数据”
- Present 用这段记录数据去验证“现在的可行性”

---

## ✦ 三、核心机制（更核心、更现实）

### 1️⃣ Present = 不可能（缺信息导致无法解锁）

- 电脑需要 `6位密码`
- 现在只提供 `前两位`（例如来自便利贴/纸条）
- `后四位`在 Present 中**不回显**，玩家需要手动再输入
- 因此 Present 的“解锁条件”对玩家来说是：**需要你在 Past 里写下的那串后四位**

### 2️⃣ Past = 可能（可修改的数据）

- 玩家在同一房间的“旧版/过去时刻”进入一个轻量 UI：
  - 类似“思考圆/对话气泡”
  - 用于输入 `后四位`
- Past 输入的内容会作为“记忆记录数据”写入本轮状态

### 3️⃣ 回到 Present = 结果验证（解锁）

- 玩家回到 Present
- 电脑检测玩家输入的 `6位` 是否与 Past 记录数据一致
- 成功后触发现实变化（灯光/电脑界面/便签细节）

---

## ✦ 四、最小谜题闭环（可试玩链路）

```text
现在（Present：电脑锁住，不回显后四位）
→ 进入过去（Past：同场景旧时刻 + 思考圆输入后四位）
→ 回到现在（Present：电脑需要6位，玩家手动再输入）
→ 系统验证通过（过去的数据影响现在的可行性）
→ 出现现实变化 + 情绪反馈
```

---

## ✦ 五、场景结构（Scene*2 + 近景*1）

### 🎮 Scene 1：Present（现在）

定位：近景优先，认知直白。

内容建议：
- 电脑（锁住，需要 6 位）
- 便利贴/纸条（显示前两位，例如 `AB`）
- 灯光较暗/冷一点（用于表明“现在”与“过去”不同，但不引发超自然联想）

不合理点（但要现实）：
- 电脑提示需要 6 位，而玩家在当下只看得到前两位
- 后四位由 Past 记录提供，但不会自动回显

交互：
- 输入 6 位（玩家自填）
- 成功/失败都有明确反馈
- 允许失败重试，但要避免刷屏成本（见“成功标准”）

入口设计（解决“切换过去/现在”设定冲突）：
- 玩家在 Present 点击电脑界面上的 `设置`（或 `思考圆` 图标）
- 点击后不需要再通过“外部入口”，而是直接进入同一场景的 `Past 旧版时刻`
- Past 结束后自动回到 Present，让玩家继续完成 `6位输入验证`

---

### 🎮 Scene 2：Past（过去）

定位：同场景旧版，但只承担“写入记录数据”的任务。

内容建议：
- 电脑仍在（但在过去阶段不以“解锁”为主目标）
- 便利贴/纸条仍显示前两位（保持认知锚点一致）
- 进入一个“思考圆 UI”（近景*1），用于编辑后四位

交互（唯一核心操作）：
- 玩家手动输入后四位（`第3-6位`）
- 提交后写入“记忆记录数据”
- 提交后自动返回 Present（或在 `提示气泡` 下方显示“返回现在”按钮）

---

### 🎮 近景*1：思考圆 UI（输入后四位）

要求：
- 视觉反馈明确：输入框/光标/提交按钮（或圆形确认手势）
- 文案尽量少，但要让玩家知道“你正在写下一个记录”
- 不需要解释时空规则

输入范围：
- 后四位：允许任意数字（允许每位玩家答案不同）

---

## ✦ 六、状态变量与因果链（用于设计落地）

建议用最小状态表达“过去数据 -> 现在验证”：

- `memory.passcodePrefix2`（Present 固定的前两位，例：AB；可写死）
- `memory.passcodeTail4`（Past 玩家输入的后四位；每个玩家不同）
- `memory.passcodeFull6`（Present 验证用：Prefix2 + Tail4）

因果链：
1. 玩家在 Past 写入 `Tail4`
2. Present 电脑验证输入 `Prefix2 + 玩家手动输入Tail4`
3. 验证通过 -> 解锁 -> 触发现实变化

---

## ✦ 七、现实变化（成功后的反馈）

目标：让玩家清楚感到“过去影响现在”，但不靠超自然。

推荐变化（从轻到强选 2-3 项）：
- 电脑界面从“锁定状态”切到“已解锁/欢迎界面”
- 便利贴文字从“只显示前两位”切到“出现少量额外标记”（不要回显完整后四位，避免取消记忆谜题）
- 灯光变亮/色温回暖（与以前对比明显）
- 轻微屏幕抖动或提示“记录已同步”（极短）

---

## ✦ 八、为什么这是“谜题”（而不是演示按钮）

谜题成立的原因在于：
- Present 阶段不提供答案：后四位不回显
- 玩家必须依赖 Past 自己输入的内容（工作记忆/短时记忆）
- 成败可验证，且与“你在 Past 做过的事”强绑定

这满足：
- 有可操作目标
- 有失败状态（输错无法解锁）
- 有明确因果反馈（解锁后世界变化）

---

## ✦ 九、成功标准（Demo 2.0 验收）

原型成功如果：
- 玩家能完成一次闭环：Past 写入 -> Present 再输入 -> 解锁
- 解锁后玩家能说出或表现出理解：
  - “我之前写过某段信息，所以现在能解锁”
- 玩家愿意再试一次（至少 1 次）以验证自己的记忆/输入

可观察指标（试玩后快速判断）：
- 失败时能否快速意识到“错在后四位记忆”
- 解锁后是否有“确实改变了”的感觉（电脑界面 + 灯光就够）

---

## ✦ 十、范围控制（Demo 纯度：不扩展）

本版本不做：
- 多种替换类型/多物体联动
- VideoCall、USB“身体=接口”的扭曲叙事（可作为后续章节）
- 多轮随机密码系统（先用“每玩家 Past 输入Tail4 -> Present验证”的单一链路）
- 完整 UI 系统（思考圆 UI 只做输入与提交）

---

## ✦ 十一、开发建议（最小实现优先级）

优先级从高到低：
1. Present：电脑需要 6 位 + 可输入 + 成功/失败反馈
2. Past：同场景切换 + 思考圆 UI 输入后四位 + 写入状态
3. 返回 Present：后四位不回显 + 玩家手动输入验证通过后触发变化

---

## ✦ 十二、可替换但不改变机制的物料建议

如果美术/贴图暂时不足，可以用占位：
- 电脑锁定贴图 / 解锁贴图切换
- 思考圆 UI 用简易输入框 + 提交按钮
- 便利贴用两行文本：第一行显示 `AB`，第二行留空提示“后四位需要记住”

